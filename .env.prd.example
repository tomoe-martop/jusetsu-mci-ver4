# 本番環境用の環境変数
# このファイルをコピーして .env.prd として使用してください
# cp .env.prd.example .env.prd

# MySQL接続情報
MCI_MYSQL_USER=your-mysql-user
MCI_MYSQL_PASSWORD=your-mysql-password
MCI_MYSQL_DATABASE=your-database-name
MCI_MYSQL_TIMEZONE=Asia/Tokyo

# Cloud SQL接続設定
# Cloud SQL Proxyを使用する場合（推奨）
USE_CLOUD_SQL=true
CLOUD_SQL_INSTANCE=housing-equipment-dashboard:asia-east1:production-dashboard-db-80

# プライベートIPで直接接続する場合は、以下のコメントを解除してUSE_CLOUD_SQLをfalseに設定
# USE_CLOUD_SQL=false
# MCI_MYSQL_HOST=10.x.x.x

# Energy Gateway API
API_SHARED_PASSWORD=your-api-shared-password

# ログレベル（オプション）
LOG_LEVEL=ERROR

# Cloud Storage設定（ログ・データファイル保存先）
# 設定しない場合はローカルのlogフォルダに保存されます
# バケット内のフォルダ構成:
#   - logs/: predictor.logファイル
#   - data/: 入力CSVファイル
GCS_LOG_BUCKET=your-log-bucket-name