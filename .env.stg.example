# ステージング環境用の環境変数
# このファイルをコピーして .env.stg として使用してください
# cp .env.stg.example .env.stg

# MySQL接続情報
MCI_MYSQL_USER=app_user
MCI_MYSQL_PASSWORD=your-mysql-password
MCI_MYSQL_DATABASE=dashboard_db
MCI_MYSQL_TIMEZONE=Asia/Tokyo

# Cloud SQL接続設定
# Cloud SQL Proxyを使用する場合（推奨）
USE_CLOUD_SQL=true
CLOUD_SQL_INSTANCE=housing-equipment-dashboard:asia-east1:he-dashboard-db-80

# プライベートIPで直接接続する場合は、以下のコメントを解除してUSE_CLOUD_SQLをfalseに設定
# USE_CLOUD_SQL=false
# MCI_MYSQL_HOST=10.x.x.x

# Energy Gateway API
API_SHARED_PASSWORD=your-api-shared-password

# Energy Gateway API URL（オプション）
# APIの向き先を変更したい場合のみ設定（デフォルト: https://api.energy-gateway.jp/0.2/estimated_data）
# ENERGY_GATEWAY_API_URL=https://stg-api.energy-gateway.jp/0.2/estimated_data

# モックAPI URL（spid=9991の場合のみ使用）
MOCK_API_URL=https://mock-api-stg-47eqsnsufa-an.a.run.app/0.2/estimated_data

# ログレベル（オプション）
LOG_LEVEL=DEBUG

# Cloud Storage設定（ログ・データファイル保存先）
# 設定しない場合はローカルのlogフォルダに保存されます
# バケット内のフォルダ構成:
#   - logs/: predictor.logファイル
#   - data/: 入力CSVファイル
GCS_LOG_BUCKET=stg-mci-ver4